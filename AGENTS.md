# 开发 agent 工作守则（PerturbPilot）

凡是在本仓库写代码或文档的 agent，都要遵守本文件。用户（仓库所有者于昌杰）在对话里给出的当次指令优先于本文件。

用词约定：**必须** = 不做就不合格；**禁止** = 做了就不合格；**应当** = 除非有写明的理由；**可以** = 自己决定。

## 0. 这是什么

PerturbPilot 是**基于 DSH（DeepSeek Harness）开发的生物科学发现框架**，不是独立的 Python 框架。形态是：

- `plugin/`：一个 DSH 插件（纯 ESM JavaScript，不打包、不转译），框架核心都在这里。
- `services/ppsvc/`：Python 写的外部服务（v0.1 是合成 oracle 和 GP-UCB 决策模块），插件经 HTTP 调用。以后换真实决策模块或 oracle，只换服务，不改插件的接口。
- 浏览器面板：`plugin/client.js`（DSH web 端右侧栏的一页）+ `plugin/lib/panel.js`（同源路由 `/perturbpilot/api`）。

已定的设计（改动前先问用户）：

- 一个任务的所有轮都在**同一个 DSH 会话**里；一轮 = 一个 DSH turn，本轮提交后由框架用 followup 开下一轮。
- **agent 选、框架提交**：agent 调 `pp_get_decision` 拿推荐，用 `pp_submit_selection` 直接交一批（正好 `batch_size` 个，候选不够时交剩下的全部），推荐以外的候选要分组写理由；框架把批次交给 oracle，再把读数回灌给决策模块。
- 任务来自仓库外的任务包（服务用 `--tasks` 读一个目录下的全部任务包，或用 `--task` 只读一个），隐藏读数放在任务包外的另一个目录（`--hidden-root` 下同名子目录 / `--hidden`），只有 oracle 读；决策模块每轮必须调用，方法不限于 GP-UCB。同一时间只跑一个任务。
- 任务只能由用户在面板上确认开始（`POST /sessions/<id>/start`，可带任务、方法和预算）：用户直接在面板上选，或者 agent 用 `ask_user_question` 问清楚后用 `pp_propose_task` 提议、用户在面板上确认。agent 的工具只能提议，不能开始；没有对得上的任务包时照实说，不编任务。
- 查文献用 DSH 的 `web_search` / `web_fetch`，只记录不拦截，事后审计。
- 科学状态存在会话之外（`runs/<会话 id>/`），每一步经 `systemPrompt.context` 注入简报，上下文压缩不丢。
- 模型原始请求/响应靠包住全局 fetch 抓取，挂在最近一次 `agent/request` 上。
- UI 首先是人和 agent 交互的界面，其次才是看记录。v0.1 的交互 = 发起任务、随时对话、暂停/继续/终止；直接干预选择和标注放到 v0.2。
- 版本规划：v0.1 闭环 + 记录；v0.2 仲裁学习 + 经验库 + 回放分叉；v0.3 token 级 trace / 训练导出。

启动方式、运行记录各文件的格式、配置键见 `plugin/README.md`。`TECHNICAL-ROUTE.md` 是早期背景材料，不是规范。

## 1. 用词

代码、注释、文档、提交信息里**禁止**出现这些旧代号：C1、C2、C3、B 套、Phase 0、L0/L1/L2、"职责①②③"。用白话：决策模块、自我更新、框架、oracle、轮、回执、配对审计。

## 2. 硬规则

**W-1 只做用户在当次对话里要的事。** 不顺手扩范围；觉得该做的另一件事，写进交付说明里建议，不直接做。

**W-2 外面看得见的接口不自己定。** 包括：工具名与参数（`pp_*`）、`runs/` 下的文件名和字段、事件类型和字段、服务的 HTTP 路径与请求/响应格式、面板路由、插件配置键、环境变量、目录名、依赖。
- 要新增或改动这些：先停下来，给出 2–3 个选项和推荐，等用户决定。
- 只影响模块内部、外面看不见的细节（私有函数、内部数据结构、算法实现）：可以自己定，在交付说明里写明。
- **禁止**绕过已定格式：往已有记录里塞没约定过的键、改变字段含义、用字符串拼接偷带结构。

**W-3 以 DSH 为底座，不重做它已有的东西。** 会话持久化、上下文压缩、审批、子 agent、技能加载、web 服务都用 DSH 的。插件只通过 DSH 公开的扩展点接入（工具注册、`systemPrompt`、事件钩子、`webServer`、client 模块）。
- DSH 版本以插件里装的 `@deepseek-ai/dsh` 为准（当前 0.1.7-rc.1）；查 DSH 行为读 `plugin/node_modules/@deepseek-ai/*` 或 `.upstream/dsh`（最新版浅克隆，gitignored），**禁止**参考归档里的旧版。

**W-4 依赖只用白名单里的。**
- 插件：Node 标准库，加 DSH 自己的包（`@deepseek-ai/cordis`、`dsh-llm`、`dsh-tools`、`schemastery`，写成 peerDependencies）。
- 服务：Python 标准库加 `numpy`；`torch` 只给 coreset / top-uncertain 两个决策方法用，按需加载。
- 开发：`pytest`、`ruff`；JS 测试用 `node --test`。
- 面板不用任何第三方库，也不用构建工具。
- 其他任何依赖都要用户批准。

**W-5 不许作假。** **禁止**：
- 把检查写成恒真；
- 在测试里 mock 掉被测对象本身（替身只能替 DSH 宿主和外部服务）；
- 删掉或放宽已有测试来让它通过；
- 手改生成物（要重新生成）；
- 把合成 oracle 的结果说成真实实验，把离线测试说成真实模型跑过。

没跑通就如实报告没跑通。

**W-6 测试规则。**
- 改代码必须带测试，下面两条命令都必须通过：
  - `cd plugin` 后 `node --test "test/*.test.js"`
  - 仓库根目录 `.venv\Scripts\python -m pytest -q`
- 默认测试**禁止**联网、**禁止**依赖密钥。需要真实模型的测试标 `@pytest.mark.live`（JS 里用环境变量显式开启），默认跳过。

**W-7 密钥只从环境变量读**（如 `DEEPSEEK_API_KEY`）。密钥**禁止**出现在文件、日志、事件、模型调用记录、测试夹具里；模型调用记录**禁止**保存请求头和响应头。

**W-8 git 规则。**
- 按版本或功能开分支，比如 `v0.1/closed-loop`。
- 提交信息写成 `<版本或模块>: <一句话>`，比如 `v0.1: 加浏览器面板`。
- **禁止** push、**禁止**改写已有历史。
- **禁止**提交 `runs/`、`.env`、`.upstream/`、`.qoder/`、`.workbuddy-ai/`、`node_modules/`。

**W-9 不从归档目录 `D:\_archive\` 复制任何代码。**

**W-10 Windows 环境注意事项。**
- 所有文本文件用 UTF-8。Python 打开文件必须写 `encoding="utf-8"`（系统默认 GBK）；Node 读写写明 `'utf8'`。
- 换行用 `\n`；Python 的 JSON 输出用 `ensure_ascii=False`。
- 路径用 `pathlib` / `node:path`，不手拼分隔符。
- 文档里的命令写成 PowerShell 能直接跑的形式，不依赖 bash 专有语法。
- 本机 `C:\Program Files\nodejs` 的 Node 22.14 跑不了 DSH 0.1.7-rc.1，要用 `D:\DeepSeek\npm-global\node.exe`（22.23）。

## 3. 不做清单

以下内容不做，除非用户点名要做：

- 审批流、预算门、权限、登录、多用户
- 跨天的长时间执行、任务队列、通用重试框架、对接湿实验仪器
- 通用组学数据平台（只写任务里真正用到的加载器）
- 工具市场、通用 MCP 接入
- 多 agent 编排、子 agent
- 任何模型训练代码，包括 RL 和 DPO（到 v0.3 也只做导出格式和转换器空壳）
- 经验库的自动晋升
- DSH 升级检查的自动化
- 面板里的多 run 对比；v0.1 里的直接干预选择和标注（放 v0.2）
- 对比实验、基线评测、为论文做的统计分析

## 4. 交付说明（每次做完交回）

```
分支 / 最后一次提交：
做了什么：<3–6 行>
改动的文件：<列表>
测试结果：<贴命令和输出的最后几行；真实模型跑没跑，如实写>
我自己定的内部细节：<没有就写"无">
需要用户决定的问题：<每个附选项和推荐；没有就写"无">
```
